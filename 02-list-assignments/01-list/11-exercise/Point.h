typedef struct { int x; int y; } point;
point soma (point, point);
void imprime (point);
int quadrado_distancia (point, point);
int passo_aleatorio ();
point passo (point);

/* definicao do tipo de dados e prototipagem das funcoes */