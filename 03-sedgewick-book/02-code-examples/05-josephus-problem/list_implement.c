#include <stdlib.h>
#include "list.h"

link list;

void initNodes(int N) {
  int i;
  link t;

  lista = malloc(sizeof *list);
  for(i = 1; i < N+1; i++){
    t = list->next;
    t = malloc(sizeof *t);
  }
  t = NULL;
}

link newNode(int x){
  link t =
  return L;
}
